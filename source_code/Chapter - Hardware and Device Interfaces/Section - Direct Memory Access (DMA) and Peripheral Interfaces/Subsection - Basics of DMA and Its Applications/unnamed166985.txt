constexpr uint32_t CIRCULAR_BUFFER_SIZE = 4096; // 4 KB circular buffer
uint8_t circularBuffer[CIRCULAR_BUFFER_SIZE];

// Configure DMA controller for circular buffer
*dmaSourceReg = reinterpret_cast(circularBuffer);
*dmaDestReg = reinterpret_cast(circularBuffer);
*dmaSizeReg = CIRCULAR_BUFFER_SIZE;
*dmaControlReg = 0x05; // Start DMA transfer in circular mode

// Process data continuously
while (true) {
    // Handle data in the circular buffer
}