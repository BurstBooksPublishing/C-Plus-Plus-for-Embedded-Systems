#include 

int main() {
    constexpr int max_iterations = 1000;
    int sum = 0;

    for (int i = 0; i < max_iterations; ++i) {
        sum += i;
    }

    std::cout << "Sum: " << sum << std::endl;
    return 0;
}