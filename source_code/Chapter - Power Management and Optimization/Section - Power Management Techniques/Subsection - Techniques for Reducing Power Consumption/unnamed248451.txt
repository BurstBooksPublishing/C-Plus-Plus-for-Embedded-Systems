void enable_clock_gating() {
    // Disable clock for unused peripherals
    asm volatile ("CLI"); // Disable interrupts
    *reinterpret_cast(0x40020000) &= ~(1 << 3); // Example register for gating
    asm volatile ("SEI"); // Enable interrupts
}