#include 

void compress_data(const std::string& data) {
    // Example of compressing data
    uLongf compressed_size = compressBound(data.size());
    std::vector buffer(compressed_size);
    compress(buffer.data(), &compressed_size, reinterpret_cast(data.data()), data.size());
}