void watchdogHandler() {
    // Reset system or log error
}

void mainLoop() {
    while (true) {
        feedWatchdog();
        if (!processTasks()) {
            watchdogHandler();
        }
    }
}