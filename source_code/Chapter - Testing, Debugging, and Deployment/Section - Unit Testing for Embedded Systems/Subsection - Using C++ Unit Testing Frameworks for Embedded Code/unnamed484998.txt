TEST_CASE("DigitalFilterTest", "[filter]") {
    auto [input, expected] = GENERATE(
        std::make_tuple(0.0f, 0.0f),
        std::make_tuple(1.0f, 0.5f),
        std::make_tuple(-1.0f, -0.5f)
    );

    DigitalFilter filter;
    REQUIRE(filter.process(input) == expected);
}